package com.frontanilla.alejandria.modules;

public class NateS {
/*
    // In create:
    assetManager = new AssetManager();
...
// Also in create, tell it what to load:
        assetManager.load("path/tostuff.png", Texture.class);
assetManager.load("path/tomorestuff.fnt", BitmapFont.class);
...
// In render call this each frame so it can load stuff:
        assetManager.update();
...
// Next you can do this to check if your shit is loaded...
        System.out.println(assetManager.getProgress() + "%");
if (assetManager.getQueuedAssets() == 0) System.out.println("Done!");
...
    // Now you can get your shit.
    Texture texture = assetManager.get("path/tostuff.png", Texture.class);
    BitmapFont bitmapFont = assetManager.get("path/tomorestuff.fnt", BitmapFont.class);
...
// You can remove stuff. Everything is ref counted, so only actually gets disposed if no other things need it.
        assetManger.remove("path.tostuff.png");
...
// Smoke all your assets.
        assetManger.dispose();*/
}
